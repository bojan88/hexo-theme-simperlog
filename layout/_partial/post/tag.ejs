<% if (post.tags && post.tags.length){ %>
  <ul class="article-tag-list">
    <% post.tags.forEach(function(tag) { %>
      <li class="article-tag-list-item"><%= tag.name %></li>
    <% }) %>
  </ul>
<% } %>
